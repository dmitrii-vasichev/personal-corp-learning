# 📝 Лог обучения Personal Corp

> Хронологический журнал всех сессий обучения. Каждая запись — что изучили, что сделали, заметки.

---

## Сессия #28 — 2026-03-02

**Модуль:** 7 — Оркестрация и Personal Corp
**Урок:** 7.2 — GitHub Agentic Workflows: автоматизация через Markdown

**Что сделали:**
- Разобрали концепцию Agentic Workflows: автоматизация через Markdown-инструкции вместо YAML-логики
- Сравнили классические GitHub Actions (детерминированный скрипт) с Agentic Workflows (агент принимает решения)
- Изучили архитектуру: YAML = триггер + permissions, Markdown = бизнес-логика на естественном языке
- Разобрали 4 паттерна: Auto-Implement, Auto-Review, Comment Commands, Stale Cleanup
- Изучили ключевой компонент `anthropics/claude-code-action@v1` — GitHub Action для запуска Claude
- Создали Agentic Workflow для Task Manager: `agentic.yml` (триггер на Issue) + `on-issue-created.md` (промпт для Auto-Implement)
- Исправили две проблемы: добавили явные `permissions` и запреты "НЕ мержь / НЕ аппрувь" в промпт
- Обсудили почему auto-implement + auto-merge — опасно: компаундирование галлюцинаций, отсутствие контрольной точки, каскадный blast radius

**Ключевые инсайты:**
- Логика стала человекочитаемой и живёт отдельно от инфраструктуры: YAML — "когда и с какими правами", Markdown — "что делать"
- Порядок шагов в Agentic Workflow не предопределён — он emergence от LLM. Это и сила, и риск
- `permissions` в YAML — тот же принцип наименьших привилегий, что и `--allowedTools` в headless-режиме
- Отсутствие запрета в промпте = неявное разрешение. Агент буквально исполняет инструкцию — явные "НЕ делай X" обязательны
- Правило безопасности: auto-implement + human review = хорошо, auto-implement + auto-merge = каскадный отказ

**Время:** ~30 мин
**Следующий шаг:** Модуль 7, Урок 7.3 — Финальная практика: полный цикл Personal Corp

---

## Сессия #27 — 2026-03-02

**Модуль:** 7 — Оркестрация и Personal Corp
**Урок:** 7.1 — Оркестратор: агент-менеджер, который раздаёт задачи

**Что сделали:**
- Разобрали концепцию оркестратора: агент-менеджер, который нарезает задачи, раздаёт исполнителям и контролирует
- Изучили архитектуру двух уровней: CEO → Оркестратор → Исполнители (каждый в своём worktree)
- Разобрали три подхода к реализации: ручной (модуль 5), скриптовый (bash), Claude Code как оркестратор (субагенты)
- Изучили паттерн промпта для оркестратора: роль, шаги, ограничение "не пиши код сам"
- Разобрали точки контроля CEO: декомпозиция, Issues, PR, итоговая фича
- Написали скрипт `task-manager/scripts/orchestrate.sh` — скриптовый оркестратор
- Скрипт: принимает номера Issues → создаёт worktrees → запускает `claude -p` параллельно → ждёт → выводит итог
- Обсудили улучшения: cleanup worktrees, проверка exit codes, подмена CLAUDE.md

**Ключевые инсайты:**
- Оркестратор не пишет код — он управляет: декомпозиция → Issues → запуск агентов → сбор результатов
- `&` + `wait` в bash — простейший способ параллелизма: запустил N процессов, подождал всех
- Принцип: автоматизируй рутину, но не контроль качества — CEO ревьюит Issues и PR даже при оркестраторе
- Масштаб процесса = масштаб задачи: оркестратор для опечатки — overkill, для эпика из PRD — необходимость
- Скриптовый оркестратор — баланс между простотой и автоматизацией: понятный, контролируемый, расширяемый

**Время:** ~30 мин
**Следующий шаг:** Модуль 7, Урок 7.2 — GitHub Agentic Workflows: автоматизация через Markdown

---

## Сессия #26 — 2026-03-02

**Модуль:** 6 — PRD и Spec-Driven Development
**Урок:** 6.4 — Практика: PRD для новой фичи Task Manager

**Что сделали:**
- Выбрали фичу "Дедлайны задач" для Task Manager
- Написали полный PRD по структуре из урока 6.1 (7 секций): проблема от боли команды, 4 пользовательских сценария, функциональные требования по 5 подсистемам, технический дизайн с миграцией/API/scheduler, 8 атомарных метрик
- Прошли ревью PRD: сильные стороны (проблема от боли, "вне scope" на 6 пунктов, конкретный дизайн), точки роста (большой scope, обоснование интервала scheduler, явный сброс флагов в требованиях)
- Декомпозировали PRD на 6 Issues по принципу MECE: [BE] миграция+API, [BE] scheduler, [BOT] команды, [BOT] парсинг транскрипций, [FE] веб-интерфейс, [BE+FE+BOT] интеграционные тесты
- Построили граф зависимостей с критическим путём: 1 → 3 → 4 → 6
- Прошли ревью декомпозиции: MECE соблюдён, маркировка [BE]/[FE]/[BOT], edge cases в criteria

**Ключевые инсайты:**
- Полный конвейер Идея → PRD → Ревью → Декомпозиция → Ревью Issues — работает как цепочка артефактов с точками контроля
- PRD от боли конкретного человека ("Настя напоминает вручную") сильнее PRD от технологии ("нужно поле deadline")
- Граф зависимостей определяет волны параллелизма: после Issue 1 можно запустить 3 агента одновременно (2, 3, 5)
- "Вне scope" — реальная защита: без него агент мог бы добавить recurring deadlines, Google Calendar, SLA-отчёты
- Модуль 6 завершён: от структуры PRD до полной декомпозиции эпика на атомарные Issues

**Время:** ~30 мин
**Модуль 6 завершён!**
**Следующий шаг:** Модуль 7, Урок 7.1 — Оркестратор: агент-менеджер, который раздаёт задачи

---

## Сессия #25 — 2026-03-02

**Модуль:** 6 — PRD и Spec-Driven Development
**Урок:** 6.3 — Декомпозиция: от эпика к Issue через оркестратора

**Что сделали:**
- Разобрали концепцию декомпозиции: разбиение эпика на атомарные Issues для параллельных агентов
- Изучили принцип MECE (Mutually Exclusive, Collectively Exhaustive) — задачи не пересекаются и покрывают весь scope
- Разобрали паттерны нарезки: по слоям, по сценариям, по компонентам, вертикальный слайс
- Изучили управление зависимостями между Issues (Depends on #N, blocked/ready labels)
- Разобрали роль оркестратора: читает PRD → нарезает Issues → создаёт в GitHub
- Выполнили практику: декомпозировали PRD "Приоритеты задач" на 5 Issues ([BE] модель, [BE] API, [FE] индикатор, [FE] фильтрация, [FE] форма)
- Разобрали результат: сильные стороны (MECE, проверяемые criteria, маркировка [BE]/[FE]) и точки роста (зависимости #4, фильтрация в API, явные тесты)

**Ключевые инсайты:**
- MECE — главный принцип: пересечение задач = merge-конфликты, неполный scope = незаконченная фича
- Зависимости определяют параллелизм: чем меньше зависимостей — тем больше агентов работают одновременно
- Маркировка [BE]/[FE] в title — сразу видно какой агент нужен и где возможны конфликты
- CEO не пишет Issues сам — он ревьюит результат оркестратора: правильная нарезка? Criteria чёткие? Зависимости логичные?
- Полный конвейер: Идея → PRD → Ревью → Декомпозиция → Ревью Issues → Агенты → Ревью PR → Merge

**Время:** ~30 мин
**Следующий шаг:** Модуль 6, Урок 6.4 — Практика: PRD для новой фичи Task Manager

---

## Сессия #24 — 2026-03-02

**Модуль:** 6 — PRD и Spec-Driven Development
**Урок:** 6.2 — GitHub Spec-Kit: /specify → /plan → /tasks → /implement

**Что сделали:**
- Разобрали концепцию Spec-Kit — набор из 4 кастомных slash-команд, формализующих конвейер от идеи до PR
- Создали все 4 команды в `task-manager/.claude/commands/`: specify.md, plan.md, tasks.md, implement.md
- Разобрали как `$ARGUMENTS` передаёт входные данные в команду
- Изучили цепочку: `/specify` → PRD-файл → `/plan` → план-файл → `/tasks` → Issues → `/implement` → PR
- Разобрали 3 принципа Spec-Kit: артефакт на каждом шаге, точка контроля между шагами, переиспользуемость команд
- Обсудили когда Spec-Kit — перебор (стоимость процесса vs стоимость задачи)

**Ключевые инсайты:**
- Каждый шаг конвейера оставляет артефакт (файл или Issue) — не теряется как чат-промпт
- Между шагами — точка контроля: ревью результата перед переходом дальше. Конвейер движется только по команде CEO
- Если план неполный — правишь артефакт до запуска следующего шага, а не надеешься что агент догадается
- Масштаб процесса должен соответствовать масштабу задачи: опечатка — просто делаешь, эпик на 10 задач — полный конвейер

**Время:** ~30 мин
**Следующий шаг:** Модуль 6, Урок 6.3 — Декомпозиция: от эпика к Issue через оркестратора

---

## Сессия #23 — 2026-03-02

**Модуль:** 6 — PRD и Spec-Driven Development
**Урок:** 6.1 — PRD: структура, зачем нужен, как писать

**Что сделали:**
- Изучили зачем нужен PRD: без спецификации агент угадывает, с PRD — исполняет контракт
- Разобрали структуру PRD для Personal Corp: Проблема → Цель → Сценарии → Требования → Scope → Дизайн → Метрики
- Разобрали роль секции "Вне scope" как ограничителя для агента
- Разобрали разницу PRD vs Issue: PRD описывает фичу, Issues — отдельные задачи внутри
- Написали PRD для фичи "Приоритеты задач" в Task Manager (docs/prd-task-priorities.md)
- Провели ревью PRD: сильные стороны и точки роста (атомарность метрик)

**Ключевые инсайты:**
- PRD — первый шаг в цепочке: Идея → PRD → Plan → Issues → Агенты → PR → Продукт
- "Вне scope" — секретное оружие: без него агент раздувает задачу
- Метрики должны быть атомарными — чтобы проверка была однозначной (да/нет)
- Лёгкая структура PRD (7 секций) — достаточна для Personal Corp, не нужен многостраничный документ

**Время:** ~30 мин

---

## Сессия #22 — 2026-03-02

**Модуль:** 5 — Claude Code + Issues = агентная разработка
**Урок:** 5.5 — Практика: 3 задачи параллельно через Issues

**Что сделали:**
- Создали 2 новых Issue (#39 — .gitignore для task-manager, #40 — схема данных Task entity) с acceptance criteria
- Подняли 3 worktrees: pcl-issue-21, pcl-issue-39, pcl-issue-40 — по одному на каждую задачу
- Подменили CLAUDE.md в worktree для Issue #21 (корневой CLAUDE.md наставника мешал агенту-исполнителю)
- Запустили 3 агента параллельно через `claude -p` в 3 терминалах — все отработали одновременно
- Все 3 агента создали PR: #41 (gitignore), #42 (schema), #43 (CLAUDE.md)
- Проревьюили каждый PR по acceptance criteria через `gh pr diff`
- Смержили все 3 PR — все 3 Issue закрылись автоматически
- Удалили worktrees, подтянули main

**Ключевые инсайты:**
- Полный цикл CEO: создал Issues → поднял worktrees → запустил агентов параллельно → проревьюил PR → смержил → cleanup
- 3 задачи параллельно = 3 worktrees + 3 терминала + 3 `claude -p`. Масштабируется линейно
- Подмена CLAUDE.md в worktree — необходимость, когда корневой CLAUDE.md задаёт роль наставника, а не исполнителя
- Ревью по acceptance criteria — быстрый и объективный процесс: чеклист из Issue → diff из PR → галочки
- Модуль 5 завершён: от "промпт в чат" к "3 агента параллельно по Issues" за 5 уроков

**Время:** ~30 мин
**Модуль 5 завершён!**
**Следующий шаг:** Модуль 6, Урок 6.1 — PRD: структура, зачем нужен, как писать

---

## Сессия #21 — 2026-03-02

**Модуль:** 5 — Claude Code + Issues = агентная разработка
**Урок:** 5.4 — Claude Code Hooks: автоматизация статусов

**Что сделали:**
- Создали директорию `.claude/hooks/` и скрипт `stop-move-to-review.sh`
- Скрипт читает переменную окружения `ISSUE_NUMBER` и добавляет label `status: in-review` через `gh issue edit`
- Создали `.claude/settings.json` с хуком на событие `Stop`
- Создали label `status: in-review` в репозитории
- Протестировали хук вручную на Issue #21 — label успешно добавился
- Разобрали 4 типа событий хуков: PreToolUse, PostToolUse, Stop, Notification
- Обсудили lint guard как пример PreToolUse хука — защита от пуша некачественного кода

**Ключевые инсайты:**
- Хуки — shell-команды, привязанные к событиям жизненного цикла агента. Настраиваются в `.claude/settings.json`
- `settings.json` (коммитится в git) vs `settings.local.json` (личные разрешения, не коммитится)
- Переменные окружения — способ передать контекст (номер Issue) от оператора к хуку: `ISSUE_NUMBER=23 claude -p "..."`
- Labels — простой способ сигнализировать статус; полноценное обновление Board через GraphQL API — на уровне оркестрации
- Lint (линтер) — статический анализ кода без запуска: ловит ошибки стиля, неиспользуемые переменные и т.д.

**Время:** ~30 мин
**Следующий шаг:** Модуль 5, Урок 5.5 — Практика: 3 задачи параллельно через Issues

---

## Сессия #20 — 2026-03-02

**Модуль:** 5 — Claude Code + Issues = агентная разработка
**Уроки:** 5.2 — Worktrees, 5.3 — Агент берёт Issue → делает ветку → PR → отчёт

**Что сделали:**
- Разобрали два режима Claude Code: интерактивный (`claude`) vs headless (`claude -p`)
- Изучили флаг `--allowedTools` для предавторизации инструментов в headless-режиме
- Прошли полный автономный цикл на Issue #23 (Code Review cheatsheet):
  - Создали worktree `pcl-issue-23` с веткой `docs/23-code-review-cheatsheet`
  - Подменили CLAUDE.md на конфигурацию исполнителя
  - Запустили `claude -p "..." --allowedTools "Bash(git:*)" "Bash(gh:*)" "Read" "Write" "Edit"`
  - Агент: прочитал Issue → обновил `practice/1.3-code-review.md` → коммит → пуш → PR #36
  - Столкнулись с блокировкой permissions — поняли что headless = нет интерактивного подтверждения
  - Проревьюили PR по acceptance criteria (4/4 ✅) → смержили
  - Удалили worktree
- Issue #23 закрылся автоматически при мерже

**Ключевые инсайты:**
- `-p` (headless) — агент работает без диалога. Вывод в stdout = отчёт. Это базовый "кирпичик" для оркестрации
- `--allowedTools` — принцип наименьших привилегий: разрешаешь только нужные инструменты (git, gh, файлы)
- В headless-режиме некого спрашивать — все разрешения задаются заранее через `--allowedTools`
- Паттерн CEO: выбрал Issue → worktree → `claude -p` → ревью PR → мерж → cleanup
- Fire-and-forget масштабируется: 5 Issues = 5 таких команд параллельно в 5 worktrees

**Время:** ~30 мин
**Следующий шаг:** Модуль 5, Урок 5.4 — Claude Code Hooks: автоматизация статусов

---

## Сессия #19 — 2026-03-02

**Модуль:** 5 — Claude Code + Issues = агентная разработка
**Урок:** 5.1 — Claude Code: работа с Issues через CLI

**Что сделали:**
- Разобрали концепцию: агент читает Issue → создаёт ветку → делает PR → отчитывается
- Столкнулись с первым prompt injection: попытка "игнорируй CLAUDE.md" — агент правильно отказал
- Поняли архитектурную проблему: нельзя запустить агента-реализатора в репо с CLAUDE.md "наставника"
- Создали `task-manager/` с собственным CLAUDE.md для агента-исполнителя
- Создали Issue #31 (feat: create README.md for task-manager)
- Запустили `claude` в интерактивном режиме из `task-manager/`
- Агент прошёл полный цикл: Issue → ветка `feat/31-readme-task-manager` → README.md → коммит → PR #32
- Сделали ревью PR и смержили — Issue #31 закрылся автоматически

**Ключевые инсайты:**
- CLAUDE.md — политика проекта, не подсказка: "игнорируй" в промпте = prompt injection, агент отказывает
- Локальный CLAUDE.md переопределяет родительский, если написан как конфигурация проекта
- Разрешения в Claude Code работают послойно: `gh issue:*` разрешается целиком, составные `cd && git` — отдельно (защита от атак)
- Роль разработчика в Personal Corp: создать Issue → запустить агента → проревьюить PR → смержить

**Время:** ~1 час
**Следующий шаг:** Модуль 5, Урок 5.2 — Worktrees: параллельная работа нескольких агентов

---

## Сессия #18 — 2026-03-02

**Модуль:** 4 — CLAUDE.md и контекст проекта
**Урок:** 4.4 — Практика: создать CLAUDE.md для Task Manager

**Что сделали:**
- Написали CLAUDE.md для фиктивного проекта Task Manager (Next.js 14, Prisma, PostgreSQL)
- Разобрали каждую секцию: Project, Stack, Commands, Architecture, Rules
- Обсудили правило `interface` vs `type` — поняли логику (расширяемость, читаемость ошибок), а не просто следовали конвенции
- Модуль 4 завершён полностью

**Ключевые инсайты:**
- CLAUDE.md должен содержать только правила, которые ты можешь объяснить — агент следует им буквально
- Порядок команд в секции Commands важен: агент выполнит их именно в этом порядке
- Правило безопасности про `password` — production-level thinking, который легко пропустить
- Когда инструмент предлагает правило — спрашивай "почему?", прежде чем включать его в CLAUDE.md

**Время:** ~30 мин
**Модуль 4 завершён!**
**Следующий шаг:** Модуль 5, Урок 5.1 — Claude Code: работа с Issues через CLI

---

## Сессия #17 — 2026-03-02

**Модуль:** 4 — CLAUDE.md и контекст проекта
**Уроки:** 4.2, 4.3, начало 4.4

**Что сделали:**
- Урок 4.2: разобрали структуру `.claude/` директории — `settings.json` (разрешения и конфиг), `commands/` (кастомные slash-команды), `hooks/` (скрипты на события)
- Изучили события хуков: PreToolUse, PostToolUse, Stop, Notification
- Урок 4.3: разобрали принципы выбора контекста для агента — что включать, что нет
- Сформулировали правило: контекст = ответы на вопросы нового разработчика в первые 5 минут
- Проанализировали текущий CLAUDE.md репозитория — подходит для process-проекта (нет стека, есть роль и команды)
- Урок 4.4: получили задание — написать CLAUDE.md для Task Manager (Next.js 14, Prisma, PostgreSQL)

**Ключевые инсайты:**
- `.claude/settings.json` — операционные правила агента (что можно делать), `CLAUDE.md` — контекст (что за проект и как работать)
- `commands/` — файл в папке = slash-команда: `/review`, `/spec`, `/deploy` без объяснений каждый раз
- Оптимальный размер CLAUDE.md: 50-150 строк. Больше 200 — работает против тебя
- Структура CLAUDE.md зависит от типа проекта: для кода — стек и команды, для процесса — роли и правила
- Что НЕ включать: историю изменений, детали реализации, туториалы, планы (они в Issues)

**Время:** ~1 час
**Следующий шаг:** Модуль 4, Урок 4.4 — написать CLAUDE.md для Task Manager (продолжение)

---

## Сессия #16 — 2026-03-02

**Модуль:** 4 — CLAUDE.md и контекст проекта
**Урок:** 4.1 — Структура CLAUDE.md: архитектура, стек, правила

**Что сделали:**
- Разобрали зачем нужен CLAUDE.md — онбординг-документ для агента, читается автоматически при каждом запуске Claude Code
- Изучили три места загрузки: глобальный (~/.claude/), корень проекта, подпапка
- Разобрали анатомию хорошего CLAUDE.md: 5 вопросов (что за проект, стек, команды, правила, архитектура)
- Проанализировали CLAUDE.md текущего репозитория: секции "что это", "файлы", "правила", "команды"
- Поняли что структура CLAUDE.md зависит от назначения проекта — для обучающего репо нет стека, зато есть роль наставника

**Ключевые инсайты:**
- CLAUDE.md должен умещаться в ~100-200 строк — длинный файл теряет эффект, агент читает его целиком
- Структура CLAUDE.md адаптируется под проект: для кода — стек и архитектура, для процессов — роли и правила
- Главный принцип: агент должен получить всё необходимое для работы и ничего лишнего

**Время:** ~30 мин
**Следующий шаг:** Модуль 4, Урок 4.2 — .claude/ директория: инструкции, хуки, конфиги

---

## Сессия #15 — 2026-03-02

**Модуль:** 3 — GitHub Projects и приоритизация
**Урок:** 3.4 — Практика: перенести текущие задачи на доску

**Что сделали:**
- Проверили итоговое состояние доски: #20 в In Progress, остальные открытые Issues в Backlog
- Убедились что все custom fields заполнены для всех Issues (#11, #19–#23)
- Модуль 3 завершён полностью

**Ключевые инсайты:**
- Доска — это живой инструмент: она отражает реальность, а не план. Если что-то не совпадает — нужно обновить доску, а не игнорировать расхождение
- Модуль 3 дал полный набор: Board + Kanban + Custom fields — этого достаточно для управления потоком задач в Personal Corp

**Время:** ~10 мин
**Модуль 3 завершён!**
**Следующий шаг:** Модуль 4, Урок 4.1 — Структура CLAUDE.md: архитектура, стек, правила

---

## Сессия #14 — 2026-03-02

**Модуль:** 3 — GitHub Projects и приоритизация
**Урок:** 3.3 — Custom fields: приоритет, сложность, время

**Что сделали:**
- Добавили custom field **Priority** (Single select): 🔴 High / 🟡 Medium / 🟢 Low
- Добавили custom field **Effort** (Single select): S / M / L / XL
- Заполнили оба поля для всех Issues на доске (#11, #19–#23)
- Отсортировали доску по Priority — Issues с 🔴 High всплыли наверх

**Ключевые инсайты:**
- Custom fields типа Single select — самый частый выбор для приоритета и сложности: конечный набор значений, легко фильтровать
- Effort в футболочных размерах (S/M/L/XL) — проще чем часы: не требует точности, но даёт ощущение масштаба
- Сортировка по Priority в Table view — быстрый способ понять что делать следующим из Backlog
- Priority + Effort вместе дают матрицу: High+S = делай прямо сейчас, Low+XL = можно отложить

**Время:** ~20 мин
**Следующий шаг:** Модуль 3, Урок 3.4 — Практика: перенести текущие задачи на доску

---

## Сессия #13 — 2026-03-02

**Модуль:** 3 — GitHub Projects и приоритизация
**Урок:** 3.2 — Kanban workflow: Backlog → In Progress → Review → Done

**Что сделали:**
- Добавили два новых статуса в Projects v2: **Backlog** и **In Review** через диалог "New option"
- Расставили колонки в правильном порядке: Backlog → Todo → In Progress → In Review → Done
- Разобрали реальные Issues (#11, #19–#23) и расставили по колонкам согласно состоянию
- #20 "Set up GitHub Projects v2 board" — перенесли в In Progress (активная задача модуля 3)
- Остальные открытые Issues — в Backlog

**Ключевые инсайты:**
- Statuses в Projects v2 — просто поле с вариантами, их набор полностью настраивается под процесс
- In Review — критичная колонка для агентной работы: агент создаёт PR → Issue переходит сюда → ты видишь что ждёт ревью
- GitHub Projects скрывает закрытые Issues — доска остаётся чистой автоматически
- Kanban — это поток, а не хранилище: задача движется слева направо до Done

**Время:** ~30 мин
**Следующий шаг:** Модуль 3, Урок 3.3 — Custom fields: приоритет, сложность, время

---

## Сессия #12 — 2026-03-02

**Модуль:** 3 — GitHub Projects и приоритизация
**Урок:** 3.1 — GitHub Projects v2: создание доски, виды, фильтры

**Что сделали:**
- Создали GitHub Project "Personal Corp Learning" через веб-интерфейс
- Импортировали все открытые Issues из `personal-corp-learning` при создании (галочка Import items from repository)
- Изучили вид **Board**: 6 Issues автоматически попали в колонку Todo
- Создали вид **Table**: те же данные в виде таблицы с колонками Title, Assignees, Status, Linked PR
- Попробовали фильтрацию: `assignee:@me` (все 6), `label:documentation` (4 из 6)
- Переименовали View 1 → `Board`, View 2 → `Table`

**Ключевые инсайты:**
- Project живёт на уровне пользователя, не репозитория — может агрегировать Issues из разных репо
- Board и Table — это не разные данные, а разные "линзы" над одним набором Items
- Status — встроенное поле Projects v2, именно оно определяет колонку на Board
- Фильтры в Projects используют тот же синтаксис что и в Issues: `assignee:@me`, `label:documentation`
- Имя лейбла в фильтре — полное (`documentation`), а не префикс (`docs`)
- Галочка "Import items from repository" при создании проекта — быстрый способ добавить все открытые Issues сразу

**Время:** ~30 мин
**Следующий шаг:** Модуль 3, Урок 3.2 — Kanban workflow: Backlog → In Progress → Review → Done

---

## Сессия #11 — 2026-03-02

**Модуль:** 2 — GitHub Issues как единица работы
**Урок:** 2.5 — Практика: оформить 5 реальных задач как Issues

**Что сделали:**
- Создали 2 новых Milestone: "Module 3: GitHub Projects" и "Module 4: CLAUDE.md" через `gh api`
- Создали 5 Issues с полной структурой: title, description, acceptance criteria, technical notes
- Issues #19–#23 покрывают реальные задачи из плана: cheatsheet М2, Projects доска, CLAUDE.md, task-manager, Code Review cheatsheet
- Каждый Issue создан с `--body-file`, label, milestone и assignee в одной команде
- Закрыли старый Issue #3 с комментарием-объяснением, заменив его правильным #23
- Освоили паттерн: файл в редакторе → `--body-file` (надёжнее heredoc при длинном тексте)

**Ключевые инсайты:**
- `--body-file` — стандартный паттерн для агентов: написал файл → передал в команду. Надёжнее heredoc в терминале
- Acceptance criteria должны быть проверяемыми чекбоксами (`- [ ]`), а не словами "правильно сделать"
- Закрывать устаревший Issue лучше с комментарием: объясняет почему закрыт, ссылается на замену
- 6 Issues с labels + milestones + assignees — уже можно фильтровать и планировать спринты
- Модуль 2 завершён полностью

**Время:** ~30 мин
**Модуль 2 завершён!**
**Следующий шаг:** Модуль 3, Урок 3.1 — GitHub Projects v2: создание доски, виды, фильтры

---

## Сессия #10 — 2026-02-27

**Модуль:** 2 — GitHub Issues как единица работы
**Урок:** 2.4 — Labels, milestones, assignees: организация

**Что сделали:**
- Разобрали три инструмента организации Issues: Labels, Milestones, Assignees
- Удалили 6 ненужных дефолтных labels (duplicate, invalid, question, wontfix, help wanted, good first issue)
- Переименовали `enhancement` → `feature` — стандарт индустрии
- Создали 4 новых label: `chore`, `priority: high`, `priority: low`, `agent-task`
- Создали первый Milestone "Module 2: Issues" через `gh api`
- Привязали Issue #11 к milestone и назначили assignee через `gh issue edit`
- Попробовали фильтрацию: `--milestone`, `--label`, `--assignee @me`

**Ключевые инсайты:**
- Labels бывают двух типов: категория (bug, feature, chore) и атрибут (priority: high, agent-task)
- `{owner}/{repo}` в `gh api` — не плейсхолдеры, а встроенный синтаксис автоподстановки из текущего git remote
- `@me` — шорткат для своего аккаунта в `gh` CLI
- `label:bug label:"priority: high"` — фильтрация по нескольким labels одновременно
- Вся организация (labels, milestones, assignees) нужна ради фильтрации при 50+ задачах

**Время:** ~30 мин
**Следующий шаг:** Модуль 2, Урок 2.5 — Практика: оформить 5 реальных задач как Issues

---

## Сессия #9 — 2026-02-27

**Модуль:** 2 — GitHub Issues как единица работы
**Урок:** 2.3 — Связь Issue ↔ PR ↔ коммиты через (#N)

**Что сделали:**
- Разобрали три типа связей в GitHub: упоминания (#N), автозакрытие (closing keywords), имя ветки
- Изучили полный список closing keywords: Closes/Fixes/Resolves и их варианты
- Разобрали концепцию трассируемости — возможность пройти всю цепочку Issue → Branch → Commit → PR → Merge
- Создали Issue #15 через `--body-file`, ветку `docs/15-issue-pr-links`, файл-справочник
- Коммит с `#15` создал перекрёстную ссылку (references), `Closes #15` в PR — автозакрытие
- Смержили PR #16, Issue #15 закрылся автоматически
- Проверили timeline Issue в веб-интерфейсе: видны все три события (label, commit reference, PR link)

**Ключевые инсайты:**
- GitHub различает "references" (упоминание через #N) и "will close" (closing keyword) — разные формулировки в timeline
- `--template "Task"` в `gh issue create` ищет по полю `name` из front matter, а не по имени файла — и может не работать стабильно. `--body-file` надёжнее
- Heredoc (`cat > file << 'EOF'`) пишет напрямую в файл без ограничений длины, в отличие от `--body "..."` в аргументах CLI
- Трассируемость бесплатна — достаточно следовать конвенциям (#N в коммитах, Closes #N в PR, номер в имени ветки)

**Время:** ~30 мин
**Следующий шаг:** Модуль 2, Урок 2.4 — Labels, milestones, assignees: организация

---

## Сессия #8 — 2026-02-27

**Модуль:** 2 — GitHub Issues как единица работы
**Урок:** 2.2 — Issue Templates: стандартизация задач

**Что сделали:**
- Разобрали зачем нужны Issue Templates — стандартизация структуры, чтобы ни человек, ни агент не забыли нужные секции
- Изучили два формата шаблонов: Markdown (.md) и YAML Issue Forms (.yml)
- Разобрали анатомию Markdown-шаблона: front matter (name, about, title, labels) + тело с секциями
- Создали два шаблона: `task.md` (стандартная задача) и `bug.md` (баг-репорт)
- Прошли полный цикл: ветка → коммит → PR #13 → merge
- Проверили в веб-интерфейсе: GitHub показал picker с выбором Bug Report / Task / Blank issue

**Ключевые инсайты:**
- Шаблоны живут в `.github/ISSUE_TEMPLATE/` и работают только из дефолтной ветки (main)
- Front matter (`---` блок) задаёт метаданные: name, about, labels — они отображаются в picker
- `labels: ["bug"]` в front matter — автоматическое добавление метки при создании Issue по шаблону
- `gh issue create --template task.md` — использование шаблона через CLI
- Blank issue остаётся доступным по умолчанию (можно убрать через config.yml)

**Время:** ~30 мин
**Следующий шаг:** Модуль 2, Урок 2.3 — Связь Issue ↔ PR ↔ коммиты через (#N)

---

## Сессия #7 — 2026-02-27

**Модуль:** 2 — GitHub Issues как единица работы
**Урок:** 2.1 — Анатомия хорошего Issue: описание, criteria, labels

**Что сделали:**
- Разобрали 5 элементов хорошего Issue: Title, Description, Acceptance Criteria, Technical Notes, Labels
- Проанализировали ранее созданные Issues (#1, #3, #5, #8) — все оказались "промптами", а не "контрактами"
- Написали Issue #11 по всем правилам: с проверяемыми acceptance criteria, label `documentation`, техническими заметками
- Прошли итерацию: первая версия критериев была размытой ("содержит правильное описание"), переписали на конкретные ("содержит секцию X с Y примерами")
- Освоили `--body-file` как способ передачи длинного тела Issue

**Ключевые инсайты:**
- Acceptance Criteria — самая важная часть Issue. Каждый критерий должен быть проверяемым: открыл результат → есть/нет
- "Сделать правильно" — плохой критерий. "Содержит таблицу labels с описанием каждого" — хороший
- Issue — это задание (контракт), а не сам результат. Описание результата в критериях, а не в теле Issue
- `--body-file` решает проблему с вставкой многострочного текста в терминал

**Время:** ~30 мин
**Следующий шаг:** Модуль 2, Урок 2.2 — Issue Templates

---

## Сессия #6 — 2026-02-27

**Модуль:** 1 — Основы Git Workflow
**Урок:** 1.5 — Практика: полный цикл branch → PR → review → merge

**Что сделали:**
- Прошли полный цикл самостоятельно (с направляющими подсказками): Issue → Branch → Commit → Push → PR → Review → Merge
- Создали Issue #8, ветку `practice/8-git-workflow-cheatsheet`, файл-шпаргалку, PR #9 с `Closes #8`
- Посмотрели diff через `gh pr diff`, смержили PR, убедились что Issue закрылся автоматически
- Создали файл `practice/1.5-git-workflow-cheatsheet.md` — справочник по всему Модулю 1

**Ключевые инсайты:**
- `cat > file << 'EOF' ... EOF` — heredoc, способ создать многострочный файл одной командой в терминале
- `-u` (`--set-upstream`) при `git push` — запоминает связку локальной и удалённой ветки, нужен только при первом пуше
- `gh pr merge` без номера — определяет PR по текущей ветке. Если PR несколько — покажет меню выбора
- `git pull` не нужен при линейной работе (один человек, push в main). Становится обязателен, когда merge происходит на стороне GitHub (через PR) или работают несколько участников/агентов

**Время:** ~30 мин
**Модуль 1 завершён!**
**Следующий шаг:** Модуль 2, Урок 2.1 — Анатомия хорошего Issue

---

## Сессия #5 — 2026-02-27

**Модуль:** 1 — Основы Git Workflow
**Урок:** 1.4 — Branch Protection Rules: защита main

**Что сделали:**
- Разобрали зачем нужны Branch Protection Rules (запрет push в main, принудительный PR)
- Изучили Rulesets — новый формат правил GitHub (замена старым branch protection rules)
- Сделали репозиторий публичным (rulesets бесплатны только для public repos)
- Создали ruleset через `gh api` с правилом: все изменения в main только через PR
- Проверили защиту: push в main отклонён с ошибкой `Changes must be made through a pull request`
- Откатили тестовый коммит через `git reset --soft HEAD~1`

**Ключевые инсайты:**
- Rulesets для приватных репозиториев требуют GitHub Pro — для учебного репо проще сделать публичным
- `current_user_can_bypass: "never"` — даже владелец подчиняется правилам, это важно для дисциплины
- `--accept-visibility-change-consequences` — GitHub CLI требует явного подтверждения при смене видимости репо
- `git reset --soft HEAD~1` — откатывает коммит, сохраняя файлы в staging. Полезно для отмены ошибочных коммитов

**Время:** ~30 мин
**Следующий шаг:** Модуль 1, Урок 1.5 — Практика: полный цикл branch → PR → review → merge

---

## Сессия #4 — 2026-02-27

**Модуль:** 1 — Основы Git Workflow
**Урок:** 1.3 — Code Review: как ревьюить и принимать PR

**Что сделали:**
- Разобрали зачем нужен Code Review (контроль качества, особенно для PR от агентов)
- Изучили три типа ревью: Approve, Request Changes, Comment
- Изучили команды: `gh pr view`, `gh pr diff`, `gh pr review`
- Создали Issue #5, ветку, файл `practice/1.3-code-review.md`, PR #6
- Провели ревью PR: проверили diff, прошлись по чеклисту, оставили комментарий
- Смержили PR, Issue #5 закрылся автоматически
- Обсудили интерфейсы управления: GitHub Mobile, Telegram-бот, веб

**Ключевые инсайты:**
- GitHub не даёт аппрувить свой PR — это защита от самоодобрения. В Personal Corp ты будешь ревьюить PR от агентов, и тогда Approve заработает
- `!` в bash имеет специальное значение внутри двойных кавычек — используй одинарные
- Запоминать команды CLI не нужно — важно понимать процесс (Issue → Branch → PR → Review → Merge). Команды будет выполнять агент, а ты ревьюишь через интерфейс
- GitHub Mobile уже покрывает 90% потребностей для ревью с телефона. Telegram-бот нужен только для кастомной автоматизации оркестратора

**Время:** ~30 мин
**Следующий шаг:** Модуль 1, Урок 1.4 — Branch Protection Rules

---

## Сессия #3 — 2026-02-26

**Модуль:** 1 — Основы Git Workflow
**Урок:** 1.2 — Pull Requests: создание, описание, связь с Issues

**Что сделали:**
- Разобрали анатомию хорошего PR: title, body, чеклист
- Изучили магические слова `Closes`, `Fixes`, `Resolves` для автозакрытия Issues
- Создали Issue #1 через `gh issue create`
- Создали PR #2 через `gh pr create` с правильным описанием и `Closes #1`
- Смержили PR через `gh pr merge --merge`
- Убедились что Issue #1 закрылся автоматически

**Ключевые инсайты:**
- Issues и PR делят общую нумерацию в GitHub — счётчик один на всё
- `origin` — это псевдоним для URL удалённого репозитория, не магическое слово
- `git push -u origin branch` — флаг `-u` запоминает связку, после этого достаточно просто `git push`
- `Closes #N` в теле PR — GitHub автоматически закрывает Issue при мердже

**Время:** ~30 мин
**Следующий шаг:** Модуль 1, Урок 1.3 — Code Review

---

## Сессия #2 — 2026-02-26

**Модуль:** 1 — Основы Git Workflow
**Урок:** 1.1 — Git branching: зачем и как создавать ветки

**Что сделали:**
- Разобрали зачем нужны ветки (изоляция, параллельность, защита main)
- Изучили naming conventions: `feature/`, `fix/`, `chore/`, `docs/` + номер Issue
- Освоили команды: `git checkout -b`, `git branch`, `git add`, `git commit`, `git log --oneline --graph`
- Создали ветку `feature/1-git-branching-practice`
- Создали папку `practice/` и файл `practice/1.1-branching.md`
- Сделали первый коммит в ветке, убедились что ветка ушла вперёд относительно `main`

**Ключевые инсайты:**
- `git add` — staging, объясняем Git что именно войдёт в коммит (можно коммитить часть изменённых файлов)
- `HEAD -> branch-name` в `git log` — показывает где ты сейчас находишься
- Ветка в графе видна как "ушла вперёд" от `main` — это правильное состояние для работы

**Время:** ~30 мин
**Следующий шаг:** Модуль 1, Урок 1.2 — Pull Requests

---

## Сессия #1 — 2026-02-26

**Модуль:** Подготовка
**Что сделали:**
- Прочитали и разобрали статью Сережи Рис "Из вайбкодера в CEO: Personal Corp на GitHub"
- Определили текущий уровень (Уровень 1: промпты в чат, push в main)
- Определили целевой уровень (Уровень 3: оркестратор + параллельные агенты)
- Составили детальный план обучения из 7 модулей и 30 уроков
- Создали систему трекинга прогресса

**Ключевые инсайты из статьи:**
- Issue — это контракт, промпт — это разговор. Агенты работают по контрактам.
- Главная причина структуры задач — ограничение контекста агента (он не может держать весь проект)
- Три уровня: промпт в чат → Issue как задача → оркестратор как менеджер
- GitHub Issues — единый интерфейс для управления "роем" агентов
- Spec-Kit: /specify → /plan → /tasks → /implement
- Agentic Workflows — автоматизация через Markdown вместо YAML

**Инструменты для изучения:**
- GitHub Issues + Projects
- Claude Code (CLI) + worktrees
- GitHub Spec-Kit
- GitHub Agentic Workflows (gh-aw)
- CLAUDE.md / .claude/ директория

**Время:** ~30 мин
**Следующий шаг:** Модуль 1, Урок 1.1 — Git branching

---

<!-- Новые записи добавляются сверху -->
